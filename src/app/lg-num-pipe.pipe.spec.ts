import { LgNumPipePipe } from './lg-num-pipe.pipe';

describe('LgNumPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LgNumPipePipe();
    expect(pipe).toBeTruthy();
  });
});
